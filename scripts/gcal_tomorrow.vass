# gcal_tomorrow.vass — Google Calendar: tomorrow's events
$events = gcal_tomorrow()
$parsed = ai("Elenca in modo chiaro e conciso questi eventi del calendario di domani: " + $events, true)
say($parsed)
